# Docker Compose overrides (read automatically by `docker compose`).
# See .env.docker.example for all options.

# Point the single-tenant storefront at a seeded vendor shop (3, 4 or 5).
DEFAULT_VENDOR_ID=3
